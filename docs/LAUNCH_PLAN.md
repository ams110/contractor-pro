# 🚀 خطة الإطلاق والنمو — كبلان (Kabblan)

> **ملف حيّ — مصدر الحقيقة.** افتحه بأي جلسة جديدة وقول لكلود: «اقرأ docs/LAUNCH_PLAN.md وكمّل الخطة».
> نحدّثه كل ما نخلّص بند (✅) أو نكتشف شي جديد.
>
> آخر تحديث: 2026-06-27

---

## 📸 الوضع الحالي (لقطة واقعية — من تدقيق فعلي للكود والقاعدة)
- **صفر عملاء مفعّلين خارجيين · ₪0 MRR** (المالك + حساب ديمو = ~100% من كل البيانات؛ من ٦ تسجيلات خارجية واحد فقط فعّل).
- المرحلة: **Google Play closed testing** (12–15 مختبِر) قبل production.
  > 🔴 **واقع 2026-06-26: صفر مختبِرين فعليين → عدّاد جوجل (12 مختبِر × 14 يوم) واقف على صفر.** أقرب إطلاق production = يوم إضافة 12 مختبِر + 14 يوم. هاي البوّابة الوحيدة الفعلية للإطلاق على جوجل. الدليل الكامل + رسالة تجنيد المختبِرين + كل نصوص المتجر جاهزة في **`docs/GOOGLE_PLAY_PRODUCTION.md`**.
- **فردي**، ميزانية تسويق **~₪500** = اختبار إشارة، **مش** شراء عملاء (CAC مدفوع $200–700/عميل).
- المالك **עוסק פטור** — سقف **₪120,000/سنة**؛ دخل التطبيق يُحسب على نفس السقف.
- الاسم: **كبلان / Kabblan**. الدومين: `app.linko.services` (يبقى). package ID: `app.linko.services` (يبقى).

## 📈 المسار الواقعي بالأرقام (ثبّته بذهنك)
- **3 شهور**: 0–5 دافعين · ₪30–300 MRR.
- **6 شهور**: 5–30 دافع · ₪150–1,500 MRR (إذا التفعيل والاحتفاظ اشتغلوا).
- **12 شهر**: نجاح = تخطّي **$1k MRR** (~30–100 دافع)؛ الوسيط = أقل من ذلك (~70% من micro‑SaaS ما يوصلوا).

---

## ✅ الأولويات (مرتّبة بالأثر)

- [x] **0. توحيد الهوية → كبلان** (الجزء أ: الاسم المرئي بكل سطوح الزبون) — **تمّ 2026-06-25** · build أخضر.
  - [ ] 0أ. إعادة توليد `public/og-image.png` بالاسم الجديد (`npm run og:shot`).
  - [ ] 0ب. rebuild أندرويد + رفعة closed-testing (حتى يتغيّر اسم الأيقونة على الجهاز).
  - [ ] 0ج. تحديث عنوان/وصف Google Play يدوياً لـ«كبلان».
- [ ] **1. جلب أول ٥ مقاولين عرب حقيقيين** تعرفهم شخصياً، وتجلسهم على التطبيق ببياناتهم الحقيقية (₪0) — **الأعلى أولوية فعلياً**؛ يحقّق closed-testing + يختبر القيمة.
- [x] **2. كسر جرف التفعيل + حاسبة اكتساب** — تمّ 2026-06-25. مكوّن `SalaryCalculator` (مختبَر، يعيد استخدام `calcSalary`) يُستعمل في: **صفحة عامّة `/calculator`** (طُعم اكتساب + SEO + مشاركة واتساب) + **تفعيل بعد التسجيل** (يتحوّل لأول عامل حقيقي) + **حذف splash الإجباري**. build أخضر · 207 اختبار · prerender+sitemap. (spec+plan في `docs/superpowers/`.)
  - [x] 2أ. **نشر** — تمّ 2026-06-27. `/calculator` حيّ على الإنتاج + **زيادة**: حاسبة `/vat-calculator` جديدة + **4 صفحات مدن SEO** (`/calculator/{haifa,jerusalem,nazareth,beer-sheva}`) + ربط من الهبوط. (تفاصيل الآلة الكاملة: `docs/GROWTH_MACHINE_PLAN.md`.)
  - [ ] 2ب. **توزيع**: نشر روابط الحاسبات بمجموعات واتساب المقاولين + رسالة لأول ١٥ مقاول. (نصّ إعلانين تيك توك A عربي + C عبري جاهز ومسجّل في `docs/ads/campaign-log.md` — باقي رندر الفيديو.)
- [~] **3. إصلاح ثقب الدفع** (إلزامي قبل أي تحصيل):
  - [x] 3أ. **قفل `plan`/`trial_ends_at` server-side** — تمّ 2026-06-25 (migration `20260625120000_lock_org_plan_trial_columns`). `authenticated` صار يقدر يحدّث `name` فقط؛ `service_role` (paddle-webhook) يكتب الخطة. مؤكّد بلا باب خلفي (`get_my_organization` قارئ، الـadmin RPCs غير قابلة للنداء من العميل).
  - [ ] 3ب. **فرض حدّ العمّال/الميزات server-side** — حالياً client فقط (`usePlanStore`)؛ يلزم trigger/RPC يفحص الخطة عند `employees` insert + بوّابة/فريق.
- [ ] **4. الإحالة + لمسة واتساب يدوية** لكل مسجّل جديد بأول أسبوع (محرّك الاحتفاظ والنمو العضوي).
- [ ] **5. اختبار ₪150–200 إعلان** على تيك توك/فيسبوك — **لقياس CPC والتسجيل→التفعيل فقط**، مش للإيراد.

## 🛠️ ثقوب تقنية مرصودة (تدقيق 2026-06-25)
- ✅ ~~`organizations.plan` + `trial_ends_at` قابلة للكتابة من المالك~~ → **أُصلِح 2026-06-25** (بند ٣أ).
- 🔴 كل تقييد الميزات/حدّ العمّال **client-side فقط** → يُتجاوز بنداء PostgREST مباشر. **(بند ٣ب — لسّا)**
- ✅ ~~`rejectWorkDay`/`rejectExpense` يعملان DELETE نهائي~~ → **أُصلِح 2026-06-26**: صارا «رفض ناعم» (`status:'rejected'`) يحفظ السجلّ؛ الحالة مدعومة أصلاً بالواجهة/البوّابة/التحليلات.
- ✅ ~~`deleteProjectWithAll` غير transactional~~ → **أُصلِح 2026-06-26**: RPC `delete_project_cascade` (SECURITY INVOKER، ذرّية) بدل ٦ عمليات منفصلة.
- 🟡 `xlsx@0.18.5` ثغرة معروفة (تصدير فقط — مقبول حالياً).
- advisors: Leaked-Password Protection **OFF** · `pg_net` في public · bucket `tiandy-il-media` متبقّي من تطبيق محذوف.

## 🎯 الخطوة التالية الواحدة
**نشر الحاسبة وتوزيعها** — الكود جاهز محلياً؛ ادفعه لـGitHub (ينشر تلقائياً على Vercel)، ثم انشر رابط `/calculator` بمجموعات واتساب المقاولين وراسِل أول ١٥ مقاول. **التوزيع = المعركة الحقيقية** (المنتج جاهز؛ التسويق هو الفجوة).

---
*ملاحظة لكلود: في ذاكرة المشروع ملفّات `brand-name-kabblan` و`kabblan-traction-reality` تلخّص السياق؛ هذا الملف هو الخطة التفصيلية.*
