-- =====================================================
-- Site Units — مخطّط أرضية المبنى (footprint) لرسم الشكل الحقيقي بالمجسّم 3D
-- footprint: JSONB مصفوفة مستطيلات منسّقة (إحداثيات 0..1) تُكوّن صورة ظلّ
--   البيت الحقيقي بدل الصندوق المربّع: [{ "x":0, "z":0, "w":1, "d":0.6 }, ...]
--   تُوضع على وحدة العمارة/الدار؛ كل طوابقها تُرسم بنفس الشكل.
-- =====================================================

ALTER TABLE project_site_units
  ADD COLUMN IF NOT EXISTS footprint JSONB;
