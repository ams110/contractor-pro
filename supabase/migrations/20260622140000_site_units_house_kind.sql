-- =====================================================
-- Site Units — دعم «الدار المستقلة» (house) جنب «عمارة الشقق»
-- عمود kind يميّز نوع الوحدة:
--   building: عمارة شقق (الطوابق تحتها شقق) — السلوك القديم (kind فارغ = هذا).
--   house:    دار مستقلة — الطوابق نفسها وحدات تتبّع (تشيك‑ليست البنود الخمسة
--             مباشرةً على الطابق، بلا شقق). يُوضع على العمارة وعلى طوابقها.
-- =====================================================

ALTER TABLE project_site_units
  ADD COLUMN IF NOT EXISTS kind TEXT;
