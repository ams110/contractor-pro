-- =====================================================
-- Site Units — المخطط ضد الواقع (Plan vs Reality)
-- تواريخ جدول التنفيذ لكل عمارة (أو أي وحدة): البداية والهدف.
-- منهما يُحسب «التقدّم المتوقّع» بالتاريخ مقابل «التقدّم الفعلي» المحسوب
-- من تقدّم الشقق/البنود → متقدّم / على المسار / متأخّر (دالة نقيّة بالواجهة).
-- =====================================================

ALTER TABLE project_site_units
  ADD COLUMN IF NOT EXISTS start_date  DATE,
  ADD COLUMN IF NOT EXISTS target_date DATE;
